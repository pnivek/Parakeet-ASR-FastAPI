venv
__pycache__/
*.pyc
.pytest_cache/

# Test fixtures are downloaded on demand, not checked in
tests/fixtures/
tests/results/

# Claude runtime state
.claude/

# Web client build outputs (Vite builds app/web/ → app/static/, both transient)
app/web/node_modules/
app/web/dist/
app/static/
